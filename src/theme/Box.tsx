import styled from "@emotion/styled";
import {
  color,
  space,
  layout,
  flexbox,
  border,
  typography,
  shadow,
  background,
  position,
} from "styled-system";

const Box = styled("div")(
  color,
  space,
  layout,
  flexbox,
  border,
  typography,
  shadow,
  background,
  position
);

export default Box;
